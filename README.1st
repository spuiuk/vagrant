Before you proceed, install vagrant using README.vagrant. Also read README.ansible and install ansible.

When creating a new test machine, create a new folder within the windows directory, add the Vagrantfile and windows_server.yml file to the new folder. Edit Vagrantfile and uncomment the nodes you want to start in the nodes directory. Change any values for a node as you require.
